1. list도 가능

- inordered list도 가능하고

_이탤릭체_ 도 가능  
**볼드체** 가능

새 리포지토리후에 ctrl + shift + ` 으로 터미널을 켰고 터미널 오른쪽의 +를 눌러 git bash로 바꿈

# HTML / CSS / JS

- 셋 중에서 프로그래밍 언어는 JS밖에 없다.

## HTML

- Hyper Text Markup Language

## CSS

- Cascading Style Sheet 스타일 언어

## 배치하는 HTML / 꾸며주는 CSS / 동작시키는 JS

웹 사이트를 만드는 것은 크게 세 가지 작업으로 이루어진다.

1. 텍스트와 이미지, 버튼 등의 요소들을 어떤 웹 페이지 공간에 배치하는 일
2. 색상, 서체, 글자 크기, 배치, 외곽선 등 각 요소의 시각적인 부분을 조정해서 보기 좋게 꾸며주는 역할
3. 특정 요소들에 기능을 집어 넣는 일
   - 예를 들어 버튼을 누르면 숫자가 올라간다든지 하는 웹사이트들의 개별 요소들에게 특정 '업무를 시키는' 역할

## HTML + CSS의 결과물 = '문서'

Java를 배우면서 작성한 것은 'code'라는 표현을 썼지만, HTML + CSS 로 작업한 것은 '문서'라고 한다. 즉, 둘을 사용해서 만든 것은 프로그래밍이 아니라는 의미다.
웹 페이지에 들어갈 요소들을 배치하고 디자인하는 정도인 .html 파일과 .css파일은 브라우저라는 프로그램(chrome 등)에서 불러와 보여주는 문서(document)라고 생각하자.
다만 JS역시 서버로 부터 전달 받아 브라우저에서 동작하는 파일이다. 차이가 있다면 추후 배울 Node.js 등 컴퓨터에 설치된 JS엔진에서 python과 같은 언어처럼 각종 복잡한 알고리즘을 정의할 수 있고
컴퓨터에 명령을 내릴수있다는 것이다.

이상의 이유로 JS는 프로그래밍 언어, JS로 작성한 것은 Code지만 HTML + CSS의 결과물은 문서라는 표현을 쓴다.

### 웹퍼블리셔 vs. 웹 개발자

웹 디자이너가 작업한 결과물을 HTML + CSS문서로 작성하는 사람들을 우리나라에서는 웹퍼블리셔라고 한다. 여기에 JS까지 적용해서 기능을 만들어 낼수있는 사람을 웹 개발자.

# .html

`!+tab` 으로 자동 생성되는 것은 웹 개발 시에 기계 혹은 시스템에 전달하는 각종 정보로, **HTML 문서 작성시에 꼭 필요한 내용들을 담고있다.**
문서 제목, 작성자, 사용 언어 및 각종 메타 정보 등으로 구성되는데, 형식자체는 동일하기 때문에 매번 부등호 부터 시작해서 일일이 작성할 필요가 없도록 단축키를 지원한다.
이를 Emmet이라고 하는데 `! + tab` 도 이 기능 중 일부에 해당한다. 앞으로 에밋이라고 했을 때, 특정 문서 작성 방식을 빠르게 해주는 단축키정도로 인지하자.
특히 `!+tab` 은 .html 파일을 생성할 때마다 쓸 예정.

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Hello, HTML</title>
  </head>
  <body>
    <h1>Hello, HTML!</h1>
  </body>
</html>
```

## `<html lang="en">` 의 의미

lang 속성은 HTML 웹사이트가 어떤 언어로 되어있는지를 컴퓨터에게 알려주는 역할을 한다. 웹페이지에서 검색할 떄 사용자들이 한글로 된 웹사이트를 찾을 수 있다.

# 첫 태그 사용하기

`<>` 모양의 부등호로 감싼다.
웹사이트의 여러가지 요소들에게 `태그(tag)`는 이 요소는 이러한 역할을 한다고 웹을 구송하는 요소의 역할을 정의하거나 정보를 알려주는 역할을 한다.

## 태그 사용방식

```html
<head>
  내용
</head>
```

```html
<body>
  태그 다음은 들여쓰기하는 것을 권장
</body>
```

`<meta charset="UTF-8">` 처럼 태그 내에 내용을 써주는 경우도 있다.

`<br>`(line break) : 줄바꿈. 닫는 태그없이 단독으로 사용가능하며 `<br/>` `<br />` 로도 사용가능

### 가로줄 태그와 공백 태그

`<hr>` : 가로줄 태그, 닫는 태그없음 <br>
`#nbsp` : 강제 공백 / html문서 상에서는 스페이스 바 / 엔터 키 / 탭 키는 코드의 가독성을 위해서만 사용되지 웹페이지에서는 기능이 없기 때문에 강제로 삽입해야할 때 사용한다. 있다는 것만 알아두자

### 주석(comment)

```html
<!-- 주석처리를 하려면 ctrl + / -->
```

```java
// 주석 처리를 하려면 ctrl + /
```

html 상에서의 주석은 개발자가 프로그램을 개발하면서 이 부분은 어떤 코드라고 설명해두거나 해당 부분은 나중에 수정할 것이라는 등의 정보를 기록해놓는 용도 -> 페이지 소스보기로 주석보기가능

## 텍스트 관련 태그

### 스타일 강조를 위한 태그

h태그로 제목 텍스트의 크기를 6단계로 조정할 수 있다.

### 첨자태그

위 첨자 / 아래 첨자
기수 혹은 서수 표현할 때 쓰는 위 첨자 :

1<sup>st <br>
h<sub>2</sub>0

1. 위 첨자 : `<sup></sup>` - superscript
2. 아래 첨자 : `<sub></sub>` - subscript

기존 글자보다 아래쪽으로 내려오는 것. 한 글자만 수정하는 경우에 css를 건드리기보다 위첨자 아래첨자 태그를 쓰는 편

### 밑줄 태그 vs. 취소선 태그

<u>사실 md파일에서도 html을 지원한다.</u>
한번 마크업 언어를 써버리면 엔터키가 안된다. 그래서 간단한 테스트는 여기서 작성, html 문서 자체가 필요하다면 그때 .html 파일을 만드는 방식으로 수업진행.
<br>
`<s>` :
<s>취소선 태그</s> (strikethrough) <br>

## 태그 속성 사용하기(속성 용어에 주목)

`<html lang="ko">`에서 lang이 마치 java Map에서의 key / "ko"가 value처럼 사용된다.

### 태그의 속성 (특성)

- 참고 : 우리나라에 html이 들어올 때는 속성으로 들어왔지만 mdn문서에서는 특성으로 공식번역 돼 버리는 바람에 혼동을 준다.
  필수적으로 속성이 요구되는 태그
  `<img src="파일경로" alt="로고">`

img 태그는 그림을 넣어주는 태그로 표시하고자 하는 이미지의 원본 파일이 필수적으로 요구된다. 원본 파일명 / 경로가 src 속성으로 들어가있고, 만약 그림 파일이 없을 경우(혹은 부득이 하게 표시되지않을 경우) 표시할 대체(alternative) 속성인 alt가 있다.

즉, 속성이란: 태그의 동작을 설정 및 제어하는 것을 목적으로 하며, 각 태그마다 설정 가능한 속성들이 개별적으로 존재한다.

### 인용문 관련 태그

`<blockquote>` 태그는 특정 텍스트를 외부에서 가져와 참조할 때 사용하는 태그로, 속성으로 cite를 가진다. 인용 내용을 들여쓰기 한다.
`cite`속성은 원전의 출처를 나타낸다.

- 참조
  태그와 속성을 한번에 작성하는 방법
  img태그는 탭 누르자마자 두개의 속성이 알아서 나왔지만, 필수가 아닌 속성들은 자동완성을 지원하지 않는다. 그런 경우에 Emmet을 활용한 작성법이 있는데, 태그입력 -> 대괄호 [ -> 속성명 -> tab을 누르면 자동완성으로 지원한다.

html에서 볼 수 있듯이 부등호를 사용하는 것은 텍스트 에디터가 이를 tag로 잘못 인식할 위험이 있다.
이를 해결하기 위해 대체 텍스트 기능을 하는 것이 있는데 이를 엔티티 이름(Entity name)이라고 한다.

`&lt;` : less than 이라는 의미로 `<`를 표기함.

`&gt;` : greater than이라는 의미로 `>`를 표기함.

`&nbsp` : non-breaking space 라는 의미로 강제 공백을 표기함.

`<q>` : (quote) 길지않은 문장일 경우 사용.

`<mark>` : 제작자가 중요하다고 생각하는 부분을 하이라이트 표시

- `<blockquote>`를 사용할 경우

<blockquote>
    blockquoote 태그
</blockquote>

<br>

- `<q>` 태그를 사용할 경우

<q>
    q 태그 사용 중입니다.
</q>

- `<mark>`를 사용할 경우

<p>
    나는 <mark>백엔드</mark> 개발자이다.
</p>

default 값으로 노란색 하이라이트가 적용되는데 css에서 다른 색깔 적용가능.
특징으로 특정 페이지에서 사용자가 특정 키워드를 검색했을 때 해당 키워드를 표시하는 용도로 사용하기도 한다.

### 머리 글자 태그

`<abbr>` : abbreviation(약어)

# 태그로 여러 요소 나열하기

1. `<ul>` : unordered list의 축약어로 순서가 상관없는 리스트를 만들 때 사용.

markdown ver.

- 사과
- 포도
- 딸기

markup ver.

<ul>
    <li>사과
    <li>포도
    <li>딸기
</ul>

```html
<ul>
  <li>사과</li>
  <li>포도</li>
  <li>딸기</li>
</ul>
```

2. `<ol>` : ordered list
   markdown ver. 1. 사과 2. 포도 3. 딸기

markup ver.

<ol>
    <li> 사과
    <li> 포도
    <li> 딸기
        <li> 여전히 무시가 되고 4가 찍힌다.
        <ol>
            <li> 이렇게 해야
            <li> 들여쓰기가
            <li> 적용된다.
            <li> 귀찮다.
        </ol>
</ol>

<br>

태그안에 태그가 들어갈 경우 안쪽 태그를 바깥쪽 태그의 자손태그라고 부른다.

`ul>li*4` : 한번에 4개 만들기

```html
<ul>
  <li></li>
  <li></li>
  <li></li>
  <li></li>
</ul>
```

## 용어의 정의를 나열하는 태그

한국어 영어 매칭을 하는 1:1 매칭과 n:n 다대다 매칭을 하는 방식의 리스트 관리를 해야할 때 사용하는 태그(잘안씀)

```html
<dl>
  <dt>용어</dt>
  <dt>정의</dt>
</dl>
```

와 같은 방식으로 작성된다.

`<dl>` : definition list의 축약어로 정의목록
`<dt>` : definition term의 축약어로 정의되어야 하는 (전문)용어
`<dd>` : definition description의 축약어로 용어의 설명 혹은 묘사

예시

```html
<body>
  <dl>
    <dt>프로그래밍</dt>
    <dd>컴퓨터 프로그래밍을 작성하는 일</dd>
    <dt>넓이</dt>
    <dt>광</dt>
    <dt>면적</dt>
    <dd>일정한 평면에 걸쳐있는 공간이나 범위의 크기</dd>
    <dt>사과</dt>
    <dd>사과나무의 열매</dd>
    <dd>자기의 잘못을 인정하고 용서를 빎</dd>
  </dl>
</body>
```

<dl>
        <dt>프로그래밍</dt>
          <dd>컴퓨터 프로그래밍을 작성하는 일</dd>
        <dt>넓이</dt>
        <dt>광</dt>
        <dt>면적</dt>
            <dd>일정한 평면에 걸쳐있는 공간이나 범위의 크기</dd>
        <dt>사과</dt>
            <dd>사과나무의 열매</dd>
            <dd>자기의 잘못을 인정하고 용서를 빎</dd>
</dl>

## 이미지 태그 사용하기

### 절대 경로 vs. 상대경로
